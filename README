#------------------------------------------------------------------------------#
# README
#------------------------------------------------------------------------------#

Set local build properties in 'build.properties'.  There is a template file
in the 'config' subdirectory.

To build, just type 'make'.
